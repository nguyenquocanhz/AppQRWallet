<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash Screen Theme for Android 12+ -->
    <style name="Theme.VíQR.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Android 12 Splash Screen API attributes -->
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_logo_qrcode</item>
        <item name="android:windowSplashScreenAnimationDuration">200</item> 
        <!-- Hide the icon if you want a plain background or match your custom splash logo -->
    </style>
</resources>